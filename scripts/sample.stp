global stack_agg

probe timer.profile {
    merged_stack = sprint_merged_stack();

    if (merged_stack != "") {
        stack_agg[merged_stack] <<< 1;
    }
}


probe timer.s($1) {
    foreach (st in stack_agg) {
        printf("%s %d\n", st, @count(stack_agg[st]));
    }
    exit();
}
